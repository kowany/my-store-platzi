import { ReplaceCommaByDotPipe } from './replace-comma-by-dot.pipe';

describe('ReplaceCommaByDotPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceCommaByDotPipe();
    expect(pipe).toBeTruthy();
  });
});
